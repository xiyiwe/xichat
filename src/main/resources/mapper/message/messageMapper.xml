<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiyiwe.xichat.dao.message.MessageMapper">
    <update id="updateFriendNotReadMessage">
        update message set isRead = '1' where receiverAccount = #{receiverAccount} and senderAccount = #{senderAccount}
    </update>

    <select id="selectPageFriendMessagesByUserAccount" resultType="com.xiyiwe.xichat.pojo.message.Message">
        select * from message where senderAccount = #{senderAccount} and receiverAccount = #{receiverAccount} limit page,10 order by createTime DESC
    </select>
    <select id="getFriendNotReadMessage" resultType="com.xiyiwe.xichat.pojo.message.Message">
        select * from message where receiverAccount = #{receiverAccount} and senderAccount = #{senderAccount} and isRead = '0'

    </select>
</mapper>
